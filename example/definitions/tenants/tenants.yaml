# Example Tenants (tenancy app)
# Tenants are referenced by slug from clusters and virtual machines.

- name: "Platform Engineering"
  slug: "platform-engineering"
  group_slug: "internal"
  description: "Owns the shared virtualization platform"
  tags: ["gitops"]
