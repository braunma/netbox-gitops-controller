# Local Terraform state and working dirs (state lives in the GitLab backend)
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Generated by cmd/tfgen — derived from inventory/virtual/*.yaml, not source
generated.tfvars.json
